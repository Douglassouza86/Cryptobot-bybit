# Configuração de dados de mercado (Fase 0 — RF-001/RF-002)
pairs:
  - BTCUSDT
  - ETHUSDT
intervals: ["5", "15", "60"]   # notação da API V5 (5m, 15m, 1h)
start: "2020-01-01"   # UTC — início do histórico
data_dir: data
